# Copy this file to .env.local for local overrides, or set these as
# environment variables in your Vercel project settings for production.

# URL of the deployed backend API (include the /api suffix).
# Example: https://ridex-backend.onrender.com/api
VITE_API_BASE_URL=http://localhost:8080/api
